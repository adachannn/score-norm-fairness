name: score_norm_fairness
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.10
  - pytorch
  - scikit-learn
  - pandas
  - matplotlib
  - h5py
  - tqdm
  - dask
  - setuptools
  - pip
  # - scipy
  # - numpy
  - pip:
    - dask
    - dask[dataframe]
    - -e . # Install this package as well
